---
description: Capture a change, idea, or bug as one well-formed Linear ticket in the right epic, with conventions applied.
argument-hint: "<what to capture>"
---
Use the **galleo-pm** subagent to run the **New ticket** SOP from `.docs/pm-process.md §3.D` for: $ARGUMENTS

Place it in the correct epic-project (E1–E14), give it the next `[E#-#]` code, write chunky scope (roll the small stuff in), set state / labels / estimate / priority per `§2`, add a `Files:` pointer, and wire a relation if it couples to another epic. If the placement or scope is ambiguous, show me the draft before creating it.
