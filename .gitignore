node_modules/
dist/
coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store

# one-off generation debug output (pnpm agent:gen)
/.gen/

# transient probe dir from scripts/check-boundaries.mjs
canvas/boundary-check-*/

# local PDF exports of the .docs markdown (generated, never the source of truth)
.docs/*.pdf
e2e/.state/
test-results/
playwright-report/

# the MCP CLI keeps a real access token here
.mcp/

# working material that needs the repo but is not part of it (see .local/README.md)
.local/
